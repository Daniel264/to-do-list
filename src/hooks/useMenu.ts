import { IconType } from "react-icons";


export interface Tasks {
  id: number;
  name: string;
  logo: string | IconType;
}

export interface Lists {
  id: number;
  name: string;
  logo: string | IconType
}